[tools]
python = "3.13"
poetry = "2.2.1"
cmake = "4.3.3"
ninja = "1.13.2"

[tasks.build]
description = "Build Qt artifacts"
env = { PYTHONUNBUFFERED = "1" }
run = [
	"poetry install --sync --no-root",
	"poetry run python -u ./build.py",
]
